# Copy this file to config/.env and fill in your own values.

# Port the Express server listens on
PORT=3000

# MongoDB connection string (local Mongo or MongoDB Atlas)
MONGO_URI='mongodb+srv://<username>:<password>@cluster0.mongodb.net/checkpointDB?retryWrites=true&w=majority'
